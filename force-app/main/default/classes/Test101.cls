public class Test101
{
public static void main()
{
 decimal p, r, t;
    decimal si;
    si=(p*t*r)/100;
    system.debug('The Simple Intrest is '+si);
    date d1=date.today()+1;
    system.debug('Today Date is '+d1);
}
}