public class VSCodeDemo {
    public void VSCodeDemo1() {
        List<Account> acc=new List<Account>();
        acc=[select id, Name from Account];
system.debug('The VS Code Demo class'+acc);
    }
}