public class Demo {
    public void disp(){
        list<Integer> N=new list<integer>{1,2,3};
    system.debug('The Values are '+N);
    }
    public Integer SI(Integer p, decimal r, decimal t){
        decimal simpleIntrest=(p*t*r)/100;
        system.debug('The Simple Intrest is '+simpleIntrest);
        return null;
    }       
}