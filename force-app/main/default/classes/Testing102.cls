public class Testing102 
{
public static void main(decimal r)
{
  decimal p=200, t=2.9, si;
  if(r==11)
  {
   si=(p*t*r)/100;
    system.debug('The SI '+si);
  }
    else
    {
        si=(p*t)/100;
        system.debug('The si' +si);
    }
}
}